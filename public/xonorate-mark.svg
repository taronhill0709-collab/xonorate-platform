<svg width="64" height="64" viewBox="0 0 64 64" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Xonorate mark">
  <defs>
    <linearGradient id="steel" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#8ea3cf" />
      <stop offset="50%" stop-color="#5b709e" />
      <stop offset="100%" stop-color="#37456b" />
    </linearGradient>
    <linearGradient id="silver" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#c7cede" />
      <stop offset="50%" stop-color="#b6bdd0" />
      <stop offset="100%" stop-color="#6b7695" />
    </linearGradient>
    <filter id="goldGlow" x="-60%" y="-60%" width="220%" height="220%">
      <feGaussianBlur stdDeviation="1.8" result="blur" />
      <feFlood flood-color="#e3ab48" flood-opacity="0.95" result="gold" />
      <feComposite in="gold" in2="blur" operator="in" result="glow" />
      <feMerge>
        <feMergeNode in="glow" />
        <feMergeNode in="glow" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>
  </defs>

  <!-- pediment roof -->
  <path d="M32 4 L58 22 L52 22 L32 9 L12 22 L6 22 Z" fill="url(#steel)" />

  <!-- entablature -->
  <rect x="8" y="22" width="48" height="4" fill="url(#steel)" />

  <!-- outer columns -->
  <rect x="10" y="26" width="5" height="26" fill="url(#steel)" />
  <rect x="49" y="26" width="5" height="26" fill="url(#steel)" />

  <!-- base / steps -->
  <rect x="6" y="52" width="52" height="4" fill="url(#steel)" />
  <rect x="4" y="56" width="56" height="4" fill="url(#steel)" />

  <!-- outer X: dark carved outline, crisp and matte, no glow -->
  <g stroke="#14203a" stroke-width="7.5" stroke-linecap="square">
    <line x1="23" y1="30" x2="41" y2="48" />
    <line x1="41" y1="30" x2="23" y2="48" />
  </g>

  <!-- inner X: shares the exact same centerline as the outer X above, so
       it's mathematically concentric. Carries the gold glow, which the
       outer dark stroke does not. -->
  <g stroke-linecap="square" filter="url(#goldGlow)">
    <g stroke="url(#silver)" stroke-width="5.5">
      <line x1="23" y1="30" x2="41" y2="48" />
      <line x1="41" y1="30" x2="23" y2="48" />
    </g>
    <g stroke="#e8ecf5" stroke-width="1" stroke-opacity="0.3">
      <line x1="25" y1="32" x2="39" y2="46" />
      <line x1="39" y1="32" x2="25" y2="46" />
    </g>
  </g>
</svg>
